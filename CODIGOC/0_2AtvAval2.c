//Davi de Sousa Costa
#include <stdio.h>

int main (void)
{
    int km;
    float combustivel, consumo;

        scanf("%i", &km);
        scanf("%f", &combustivel);

        printf("%.3f km/l", consumo = km / combustivel);
    
    return 0;
}