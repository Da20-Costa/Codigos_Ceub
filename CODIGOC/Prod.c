#include <stdio.h>

int main(void)
{
    int a, b, X;
    
    printf("Insira dois numeros inteiros: \n");
    scanf("%i %i", &a, &b);

    printf("O produto dos numeros eh: \nProd = %i", X = a * b);
}