#include <stdio.h>

int main (void)
{
    int A, B, C, D, DIFERENCA;

    scanf("%i", &A);
    scanf("%i", &B);
    scanf("%i", &C);
    scanf("%i", &D);

    printf("DIFERENCA = %i", DIFERENCA = A * B - C * D);
}